schema: grape_failure_effective_estimator/v1

# 20260612 hovering bag 4. Copy this file and update the hash/window for a
# different failed flight; the CLI never accepts a silently substituted bag.
expected_bag_sha256: bd3fc7f71797c0f5cb665acc50832da93c590e540fa170f9977182ecedf93bf8

topics:
  command: /gimbalrotor/four_axes/command
  gimbal: /gimbalrotor/gimbals_ctrl
  imu: /gimbalrotor/sensor_plugin/imu1/ros_converted
  odometry: /gimbalrotor/uav/cog/odom

estimation:
  start_offset_s: 20.0
  end_offset_s: 25.28
  sample_rate_hz: 100.0
  smoothing_window_s: 0.07
  maximum_delay_s: 0.15
  delay_step_s: 0.01
  bootstrap_samples: 300
  bootstrap_block_s: 0.25
  huber_delta: 1.5
  ridge: 1.0e-8
  seed: 7
  minimum_input_std: 0.001
  minimum_r2: 0.05
  maximum_relative_interval_width: 4.0
