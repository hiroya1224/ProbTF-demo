# PID gain region: failure1 / roll_pitch

- alpha threshold: `0.95`
- covariance mode: `conservative_fusion`
- delay mode: `fitted_thrust_delay`
- valid samples: `64` / `64`
- recorded-gain stable fraction (surrogate center): `0.515625`

## Recorded gain

- P = `20`
- I = `1`
- D = `8`

The shading on each projection shows the maximum stable fraction over the hidden gain coordinate.  The black contour is the alpha-level boundary, and the hatched/filled region corresponds to stable fraction >= alpha.
